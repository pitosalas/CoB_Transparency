<div class = "container">
<%= form_with(model: feedback) do |form| %>
  <% if feedback.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(feedback.errors.count, "error") %> prohibited this feedback from being saved:</h2>

      <ul>
        <% feedback.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  

  <div class="field">
    <%= form.text_field :topic, class: "topic", value: "general" %>
    <%= form.text_area :issue, class: "form-control", placeholder: "Please report any issues.", rows:"6", cols:"40" %>
  </div>

  </textarea>
  <div class="actions">
    <%= form.submit 'Submit Feedback', class: 'form-submission btn btn-primary' %>
  </div>
<% end %>

</div>