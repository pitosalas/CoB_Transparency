<div class = "container">
<%= form_with(model: feedback) do |form| %>
  <% if feedback.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(feedback.errors.count, "error") %> prohibited this feedback from being saved:</h2>

      <ul>
        <% feedback.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :issue%>
    <%= form.text_field :issue, class: "form-control" %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, class: "form-control", rows:"6", cols:"40" %>
  </div>

  </textarea>
  <div class="actions">
    <%= form.submit 'Submit Feedback', class: 'form-submission btn btn-primary' %>
  </div>
<% end %>

</div>