<%= form_with(model: study_area, local: true) do |form| %>
  <% if study_area.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(study_area.errors.count, "error") %> prohibited this study_area from being saved:</h2>

      <ul>
        <% study_area.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :location %>
    <%= form.text_field :location %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
